# 网盘部署（GitHub / Cloudflare 都不行的备选）

把安装包上传到网盘，获取直链后填到网站配置。

## 适合

- 蓝奏云、123云盘、阿里云盘等
- 只有网页能放 GitHub，安装包放网盘

## 步骤

### 1. 上传文件到网盘

把 `downloads/ACE.apk` 和 `2/` 里的 exe 等分别上传，**获取每个文件的下载直链**。

### 2. 编辑外链配置

打开 `js/external-links.js`，把直链填进去（文件里已有示例格式）。

### 3. 开启外链

`js/releases-config.js` 保持 `enabled: false`。

`index.html` 已引入 `external-links.js`，配置好后下载按钮会自动跳转网盘。

---

## 注意

- 必须用**直链**（点链接直接下载），不能用需要跳转的分享页
- 网盘链接可能会过期，过期后重新上传并更新配置
